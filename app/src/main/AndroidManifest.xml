<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.radhikakalaiselvan.cometcollaborate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CometLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ProfessorDetails" />
        <activity android:name=".RequestLeave" />
        <activity android:name=".FixAppointment" />
        <activity android:name=".SuccessLeaveReq" />
        <activity android:name=".Attendance"></activity>
    </application>

</manifest>